# Codex 施工简报 · Task 4.1：开放格数量改每局随机 2-3(产品拍板 2026-07-02)

> 本简报由 Claude(项目大脑/监督者)签发，是你(Codex)本批唯一授权范围。完成后由 Claude 按四层门禁验收。
> 产品变更：开放格从**固定 6 个**改为**每局随机 2-3 个**(格位稀缺化,放大"5 符选 2-3 上场"的对位决策)。本批只动数量机制,**不动路线/候选格几何、不动渲染**。

---

## 0 · 目标
使尺子 `tests/map.gate.test.ts`(Claude 已按新契约更新,当前对旧实现为红)8 用例重新全 PASS：
- `src/config/maps.json` — `randomization.openSlotCount: 6` 废除,改 `openSlotCountRange: { "min": 2, "max": 3 }`
- `src/config/schema/maps.ts` — 同步校验
- `src/game/battle/map/MapGenerator.ts` — 开格数量随机化
- `tests/config.validation.test.ts` — 补 1 条负例(此文件属你可改域)

一句话交付标准：**同 seed 复现不变的前提下,每局开格数在 [min,max] 内随机,60 个 seed 内 2 与 3 都出现。**

---

## 1 · 角色与铁律
- 只做本简报授权的事。**禁止触碰尺子**：`tests/*.gate.test.ts`、`tests/golden.formulas.test.ts`、`scripts/**`、`acceptance/**`、`docs/**`、`package.json`。施工后机器门禁自动查交集,非空即 FAIL。
- 一切随机走注入 seed 的 `createRng` 单一流;禁 `Math.random`/`Date`;`MapGenerator.ts` 维持零裸数字(min/max 读 config)。
- **无 `as any` / `@ts-ignore`**。

---

## 2 · 必读
1. `tests/map.gate.test.ts` 头注(新契约,只读)。
2. `src/game/battle/map/MapGenerator.ts` 现有实现与 `src/config/schema/maps.ts` 现有校验。
3. `acceptance/contracts.json` 的 `tasks["T4.1"]`。

---

## 3 · 交付物(精确接口)
1. **maps.json**：`randomization` 内删 `openSlotCount`,增 `"openSlotCountRange": { "min": 2, "max": 3 }`。其余字段(含 routes/candidateSlots 几何)一字不动。
2. **schema/maps.ts**：`MapConfig.randomization` 类型同步(`openSlotCountRange: { min: number; max: number }`);校验整数、`1 ≤ min ≤ max`,违反即抛;模板 `candidateSlots` 数量下限从"openSlotCount+2"改为 **`max+2`**。
3. **MapGenerator.ts**：`generateMap` 中在选模板之后、开格之前,用同一 rng 流抽 `count = min + floor(rng()*(max-min+1))`,后续开格/五行逻辑用 `count` 替代原 `openSlotCount`。`GeneratedMap` 形状不变。
4. **tests/config.validation.test.ts**：增 maps 负例——`openSlotCountRange.min > max` 的配置应加载抛错。

---

## 4 · DoD(另见 contracts.json T4.1)
1. `tests/map.gate.test.ts` 8 用例全 PASS,**未修改该文件**;既有 21+ 测试不回归。
2. `npm run quality:gate -- --task T4.1` 全绿(isolation/config tables/typescript/test/build)。

## 5 · 自检
```bash
npm run quality:gate -- --task T4.1
```

## 6 · 交付输出(结束时打印)
1. 修改文件清单(应恰为上述 4 个文件)。
2. 门禁逐阶段摘要 + map.gate 8 用例 PASS 文本。
3. 随机源声明:count 抽取读 rng 的代码位置,确认无 Math.random/Date。
